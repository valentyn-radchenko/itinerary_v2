apiVersion: v1
kind: Service
metadata:
  name: itinerary-main
spec:
  selector:
    app: itinerary-main
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
  